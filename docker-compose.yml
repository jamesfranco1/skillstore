version: '3.8'

services:
  skillstore:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - skill_data:/app/data
    restart: unless-stopped

volumes:
  skill_data:


